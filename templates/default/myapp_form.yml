login:
  email:
    - rule: NOT_BLANK
      message: "{{Input email address.}}"
    - rule: EMAIL
      message: "{{Invalid email address.}}"
  password:
    - rule: NOT_BLANK
      message: "{{Input password.}}"

